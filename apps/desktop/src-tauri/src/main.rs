// Tauri 核心入口（占位）。见 Start.md §2 / §9.2 / §13。
// 职责：窗口管理、启动 Python 后端 sidecar + 内嵌 redis-server、LAN 发现(mDNS/UDP)。
// 注意：所有可写数据必须落用户可写目录，绝不写安装目录（Start.md §1 / §4）。

fn main() {
    // TODO Phase 3: spawn python sidecar + redis-server, start mDNS/UDP discovery.
    println!("ai-tabletop desktop shell — placeholder");
}
